# Frontend build-time environment (Vite). Copy to .env and adjust per deploy.
# When unset, the app talks to /api on the same origin (reverse-proxy setup).
# Set this only when the frontend is served from a different origin than the
# backend (e.g. a CDN). Must end with a trailing slash: https://api.example.com/
VITE_API_URL=